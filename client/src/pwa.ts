// PWA initialization - disabled to avoid build errors

export async function initPWA() {
  // PWA functionality disabled in current build to avoid virtual module errors
  console.log('PWA initialization skipped - virtual:pwa-register module not available')
}
